<UserControl x:Class="SellBroCRMWPF.Auth.AuthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SellBroCRMWPF"
             mc:Ignorable="d">
    <Grid>
        <Button Name="SignInButton" Content="SignIn" HorizontalAlignment="Left" Margin="198,217,0,0" VerticalAlignment="Top" Height="74" Width="126" Command="{Binding SignInCommand}"/>
        <Button Name="SignUpButton" Content="SignUp" HorizontalAlignment="Left" Margin="474,217,0,0" VerticalAlignment="Top" Height="74" Width="129" Click="{Binding SignUpCommand}"/>
        <TextBox Name="TbEmail" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <PasswordBox Name="PbPassword" PasswordChanged="PbPassword_OnPasswordChanged"></PasswordBox>
    </Grid>
</UserControl>
